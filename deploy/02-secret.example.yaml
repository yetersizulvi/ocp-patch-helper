# Template only. Never commit the real values.
apiVersion: v1
kind: Secret
metadata:
  name: ocp-patch-agent-secrets
  namespace: ocp-patch-agent
type: Opaque
stringData:
  AGENT_TOKEN: REPLACE_WITH_CLUSTER_SPECIFIC_AGENT_TOKEN
  LLM_TOKEN: REPLACE_WITH_LLM_TOKEN
